node_modules
.next
dist
coverage
.turbo
.DS_Store
*.log
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod
# host-specific deploy override + deploy state live only on the VM
docker-compose.deploy.yml
.deploy-state
api/bin
api/.cache

# personal financial workbooks used for legacy imports — never commit these
*.xlsx
*.xls

