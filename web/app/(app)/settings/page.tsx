export default function SettingsPage() {
  return (
    <section className="heroCard">
      <span className="eyebrow">Settings</span>
      <h1 className="pageTitle">Local environment, bootstrap admin, and core setup belong here first.</h1>
      <p className="lede">Phase 1 will add account, category, business, and investment-type settings.</p>
    </section>
  );
}

